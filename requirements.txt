# Core dependencies
flask>=2.0.0
flask-cors>=3.0.10
psutil>=5.9.0
psycopg2-binary>=2.9.5
python-dotenv>=1.0.0
requests>=2.28.0
pyyaml>=6.0.0

# Optional dependencies (platform-specific)
# For Windows active window tracking
# pywin32>=303; sys_platform == 'win32'

# For macOS active window tracking
# pyobjc-framework-AppKit>=8.5; sys_platform == 'darwin'

# Data processing and visualization (optional)
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Platform-specific (as needed)
# For Windows
# pywin32>=306; platform_system == "Windows"

# For X11/Linux idle detection
# xlib>=0.21; platform_system == "Linux"